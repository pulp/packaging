apiVersion: v1
kind: PersistentVolume
metadata:
  name: etc-pki-pulp-volume
  namespace: pulp
spec:
  capacity:
    storage: 10Mi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  hostPath:
    path: /volumes/etc-pki-pulp-volume
